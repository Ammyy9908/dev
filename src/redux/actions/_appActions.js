export const setUser = (user)=>({
    type:"SET_USER",
    user
 })

 export const setDrop = (dropdown)=>({
    type:"SET_DROPDOWN",
    dropdown
 })

 export const setTab = (activeTab)=>({
    type:"SET_TAB",
    activeTab
 })